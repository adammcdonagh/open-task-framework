{
  "type": "execution",
  "hosts": [
    "{{ HOST_A }}",
    "{{ HOST_B }}"
  ],
  "username": "{{ SSH_USERNAME }}",
  "directory": "/tmp",
  "command": "df -h",
  "protocol": {
    "name": "ssh",
    "credentials": {
      "username": "{{ SSH_USERNAME }}"
    }
  }
}