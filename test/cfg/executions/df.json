{
  "type": "execution",
  "hosts": [
    "{{ HOST_A }}",
    "{{ HOST_B }}"
  ],
  "directory": "/tmp",
  "command": "df -h",
  "protocol": {
    "name": "ssh",
    "credentials": {
      "username": "{{ SSH_USERNAME }}"
    }
  }
}
