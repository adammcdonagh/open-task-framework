{
  "type": "execution",
  "hosts": [
    "{{ HOST_A }}",
    "{{ HOST_B }}"
  ],
  "directory": "/tmp",
  "command": "test -e /tmp/does-not-exist",
  "protocol": {
    "name": "ssh",
    "credentials": {
      "username": "{{ SSH_USERNAME }}"
    }
  }
}